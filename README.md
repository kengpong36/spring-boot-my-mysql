# spring-boot-my-mysql

Create Database
id (Integer): Primary Key with Auto Increment.
title (varchar(45)): The title of the Note. (NOT NULL field)
content(varchar(45)): Note’s content. (NOT NULL field)
createdAt(DateTime): Time at which the Note was created.
updatedAt(DateTime): Time at which the Note was updated.